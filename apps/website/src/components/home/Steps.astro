---
/* 7. 流程区：01/02/03 三栏，顶线分隔，无卡片。 */
const STEPS = [
  { num: "01", title: "先把我记下来", body: "建档、上传照片，陪伴天数、照片和成长节点都有自己的位置。" },
  { num: "02", title: "挑一个想试的玩法", body: "图文、AI、如果我是人、互动、短片和纪念空间，都从手机里的照片开始。" },
  { num: "03", title: "选中喜欢的那一张", body: "查看候选和生成进度，完成后保存、分享，或继续做下一件作品。" },
];
---

<section class="section" id="how">
  <div class="container">
    <div class="section-head reveal">
      <p class="eyebrow">怎么用</p>
      <h2 class="h2 h2-sm">三步，就有一张成品</h2>
    </div>
    <div class="grid-3 steps">
      {
        STEPS.map((step, index) => (
          <div class="step reveal" data-delay={index > 0 ? String(index) : null}>
            <span class="step-num">{step.num}</span>
            <h3 class="h3">{step.title}</h3>
            <p class="body-text">{step.body}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
