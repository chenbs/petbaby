# 发布验收清单

更新：2026-08-23 ｜ 范围：Web/H5/API、Worker、微信小程序与独立官网

## 发布门禁

> `.github/workflows/ci.yml` 已于 2026-08-22 删除，以下关卡当前不会由 GitHub Actions 自动执行。发布前必须逐项手工跑完；同时应恢复覆盖平台检查/E2E、PostgreSQL 迁移、小程序、官网、Gitleaks 和容器构建的等价自动门禁。

- `pnpm install --frozen-lockfile`、`pnpm audit --prod`、`pnpm check`、`pnpm test:e2e` 全部通过。
- PostgreSQL 空库执行 `pnpm db:migrate` 成功；重复执行无副作用；备份恢复演练完成。
- `/api/health` 返回 `200`、`database=true`、`stale=0`；Worker 积压与失败率告警已验证。
- Gitleaks 无真实密钥；镜像以非 root 用户运行；生产启动命令为 `next start`。
- **虚拟支付代码改造已完成，但尚未公开付费**：按 SKU 选择普通/虚拟 Provider；`growth_orders` 只能在验签回调后发权益；端上按 `clientParams.mode` 调 `wx.requestVirtualPayment`；iOS 退款问询 3 秒内响应。微信签约、商品上架、真实回调和真机验收完成后才可开放。
- `deploy/scripts/preflight.sh production` 通过：没有示例域名、默认密码、占位值或模拟 Provider，且 `APP_ENV` 不是 `staging`。
- `deploy/scripts/smoke-test.sh` 在目标环境跑通主链路（注册/上传/生成/读回存储/分享）。

### 当前本地基线（宠物人化直接效果图链路完成后）

| 门禁                                       | 结果                                           |
| ---------------------------------------- | -------------------------------------------- |
| `apps/platform` `vitest run`             | **490 用例全绿**                         |
| 覆盖率（阈值 75 语句 / 65 分支）                    | 76.33% 语句 / 69.12% 分支                        |
| 本批改动文件 `eslint` / `tsc --noEmit`     | 通过                                           |
| Playwright（PGlite）          | **4 / 4 通过** |
| Playwright（PostgreSQL）          | **4 / 4 通过** |
| `apps/miniprogram` `pnpm validate`         | **23 页 / 4 主题 / 57 token，22 项通过** |
| 迁移范围                                     | `0000`～`0029`                                |

**本地基线未覆盖、必须按本清单单独验收的**：真实 PostgreSQL 空库迁移与重复迁移、备份恢复演练、Docker 镜像实构建、远端 CI、gitleaks 全历史扫描、健康档案 PDF 的中文字形（真机/真实打印）及真实支付/微信回调。官网 `check:pixels`、`verify`、平台媒体冒烟与迁移校验已在本机完成。

历史基线记录（22 页 / 56 token、194 用例、346 用例、448 用例等）已随批次完成移除，需要追溯请查 Git 历史。

> 环境提示：若 shell 里设了 `ALL_PROXY=socks5://…`，`pnpm test:e2e` 会以
> `Protocol "socks5:" not supported` 直接失败 —— 这是 Playwright 不支持 socks5，
> 与被测代码无关。跑之前 `unset ALL_PROXY all_proxy` 即可。
> 
> 另一处：`vitest run --reporter=basic` 在 vitest 4 上会以 `ERR_LOAD_URL` 失败（`basic` reporter 已移除），用默认 reporter。

## 功能验收

- PL-01、PL-02 三种风格、PL-03 6-20 图均生成真实文件；PL-03 可下载 PDF。
- 照片库可复用；24 小时内重新生成不扣额度；作品文案可编辑。
- 高清解锁、图片/PDF 下载、创建和撤销分享均有效；撤销后的旧链接返回 404。
- 订单重复支付不重复解锁；30 分钟未支付订单关闭；失败全退与不满意退 50%规则正确。
- 免费作品 90 天清理、孤立照片清理、限频和成本熔断均在预发布环境演练。
- **年度视频需在装有 ffmpeg 的环境实际抽帧验收**（开发机无 ffmpeg，此项从未跑过）：四段齐全、日期与 `photos.shot_at` 一致、成片总长等于所选的 10/20/30 秒，照片只有 1 张时降级为单段不崩。
- 成长时间线与「去年今日」：排序按 `coalesce(shot_at, created_at)`，无 EXIF 照片不堆在最前；「去年今日」未命中时端上整块隐藏而非显示占位文案。
- 多页纪念册 PDF 内照片真实嵌入；陪伴天数为过去式且不随日期递增。
- 分档定价在**下单时**计算（不是生成时）：制作页与作品页显示的价与实际应收一致；会员直接给最高档；纪念形态不分档。
- 会员权益真实兑付：年报解锁命中权益时**不产生订单**、实体下单按折扣价收、健康档案按 `healthExportUnlimited` 或按次凭据核销；权益用尽后第二次导出被正确拒绝。

## 健康分诊线验收（红线项，做错的代价大于其他线）

十条红线全文见 `../product/16-竞品分析与产品复盘.md` 3.8。以下每条都必须实测，代码侧已有用例钉住但真实文案与观感需人工过：

- **`memorial` 宠物在健康线全部功能上被拒**：端上列表过滤 + 服务端 `HEALTH_UNAVAILABLE_MEMORIAL`，导出与 `recordCare` 写入侧同样拦。三类主动提示的 SQL 逐条带 `life_stage <> 'memorial'`。
- **不出现任何药物推荐**：`mentionsDrug` 是代码级后置过滤，命中即整段降级。抽查药名/类别/剂量/动作/中英混写的绕过尝试。
- **最低紧急度档也带升级条件**，不给「不用去医院」的确定结论。
- **紧急症状走关键词直通不进模型**，口语表述（「喘得厉害」「尿不出来」）必须命中。
- **用户可见文案不出现「诊断」「确诊」「治愈」「问诊」**：页面、推送、类目描述、官网、小程序简介全覆盖。免责声明由服务端下发、与结论同屏不折叠。
- **体重只陈述变化不做评价**：给「较上次 +10%（400 克）」，不给「偏胖」「正常范围」「BMI」。
- **健康档案 PDF 是就医准备材料不是体检报告**：免责声明在第一页顶部、带底衬、位于正文之前；不可分享（无 share_token 无公开路径）；不打 AI 标识。**中文字形需在真实容器/打印下确认不是方框。**
- **免疫记录的项目名由用户自填**，产品不给候选清单。
- 主动提示去重有效：疫苗续期、再次称重、跨季度后能再次提示（`subject_key` 带变化量）。

## 独立官网验收（`apps/website`）

- `pnpm build` → `pnpm check:links`（站内链接、canonical 与 sitemap 双向逐字一致，当前为手工发布门禁）。
- `pnpm check:pixels`：首页与 `docs/website/prototype/` 逐像素比对，三档断点（390 / 768 / 1440）在截至 `<footer>` 的范围内必须 **0 差异像素**。页脚刻意与原型不同，用结构断言单独核。
- `pnpm verify`：浏览器端 63 条断言（首屏 CLS 与图片属性、小程序码三触点的双轨交互与键盘可达、文章正文排版、solid 顶栏实测对比度、法务页与 404）。需 Chromium，改交互或样式后手动跑。
- `src/styles/site.css` 与 `src/scripts/site.js` 仍是原型文件的逐字节副本（`cmp` 可验）。
- 素材三处一致：`tools/imagegen/out/website/` → 原型 → `apps/website/public/assets/`。`release-website.sh` 只 warn 不自动同步。
- 构建时 `SITE_URL` 指向真实域名；`robots.txt` / `llms.txt` 是端点而非静态文件。
- **上线硬阻塞**：小程序码素材 `public/assets/miniprogram-qr.png` + `site.ts` 的 `MINIPROGRAM_QR.available=true`；ICP 备案；法务正文替换（半真半假的隐私政策比空页面风险更高）。

## 微信小程序验收

- 按 `04-external-prerequisites.md` 补齐 AppID、合法域名和上传私钥。
- 当前出口 IP 已加入上传白名单，`project.config.json`/CI AppID 与目标环境一致。
- `cd apps/miniprogram && pnpm install --frozen-lockfile && pnpm validate` 通过，输出页数、主题数与 token 数符合预期（当前规模见文档索引），且末尾 `node --test` 无失败。
- 开发者工具无编译错误；登录失败可恢复；上传压缩、进度和轮询退避正常。
- 真机验证相册权限、保存作品、分享卡片、订单和隐私入口。
- 低端 Android 与至少一台 iPhone 验证无横向溢出、无白屏、无超时死循环。
- 调试基础库切到下限 `2.19.2` 做一次回归：支付能力不足时显示升级提示，非支付页面不白屏。

## 主题系统验收

以下项依赖真机与开发者工具，是主题系统交付后唯一剩余的门禁（详见 `docs/demand/theme.md` 第 14 章、`theme-2.md` 第 14 章）：

- 四套主题（`cute`/`glass`/`light`/`dark`）× 全部页面走查截图集，无颜色错配与不可读文本。
- iOS 与 Android 真机各一台验证 `glass` 主题：`backdrop-filter` 生效表现，以及不支持时自动降级为不透明表面 + 更强描边。
- 玻璃面板三个档位在图片/视频两类作品上的表现，主 CTA 在三档均可见。
- 刘海屏与非刘海屏的导航栏走查；面板拖动帧率与「拖动期间零 `setData`」实测。

## 上线与回滚

- 部署前创建数据库备份；先部署迁移，再部署 Web 与 Worker；最后开放流量。
- 灰度检查登录、上传、生成、支付回调、下载、分享和退款各一单。
- 回滚只回滚应用镜像，不逆向删除兼容字段；必要时开启生成熔断并停止 Worker。
- 发布后 24 小时重点监控队列积压、生成失败率、单次成本、支付通知和媒体 404。

## 阶段二、三专项

- 普通 AI 模板返回四个候选并支持重抽；宠物人化返回两个候选且不支持重抽。两类任务的 AI 标识、成本记录、取消/失败恢复、选中结果和高清权益均正确。
- 图片货架已登记 9 个入口 / 116 个条目（76 live / 40 pending-review）；逐张验证全部 live 模板展示图返回 200 且尺寸正确、运行时参考图按注册键可读取。V2 人化图片、提示词与计划对象键的本地一致性门禁已通过；明确发布批准、对象上传和真实调用验证前继续保持 `pending-review`。旧方案中间图、主人身份原图和历史私有人形身份卡不得进入对象存储 seed。
- 双主体正式公开前至少用 12 位不同外观主人完成人工盲评；当前 4 位主人、80/80 生成成功只算工程稳定性，不算发布质量通过。
- 主人照片上传必须确认授权，列表、媒体读取和删除均校验归属；账户删除后原图对象和数据库记录均不可再访问。
- 用真实 lingsuan 验证单宠“母版 → 宠物”和双主体“母版 → 主人 → 宠物”顺序，缺参考图明确失败，定向重抽不改变未选角色和构图。
- 宠物人化用真实 lingsuan 验证输入顺序严格为“宠物原图图一 → 自有效果图图二”，一次只生成 2 张，成品均为完整自然真人；确认没有新增身份缓存/身份成本，端上不显示重抽入口，服务端重抽返回 409。效果图同一对象必须同时承担公开样图和图二，且用户明确审批前不得改为 `live`。
- 互动页的匿名访问、停留归因、撤销/过期和服务端 15 秒 MP4 导出完成预发布验证。
- 独立视频项目可保存/排序/编辑、生成预览、取消/重试、支付解锁、归档、下载和分享；FFmpeg Worker 失败不死循环。
- 纪念空间可隐藏/恢复，纪念册、纪念视频和星尘页可生成、归档、解锁、分享和撤销。
- 赛马后台可筛选指标、比较 live 基准、合法流转、回滚并保留操作历史；双端只展示同一 live 变体。
- 订阅消息按宠物授权，提前 7 天调度、频控、退订、失败重试和微信响应记录完成预发布验证。
- 实体 SKU/地址、支付退款、印刷 PDF/QC、物流和管理员人工履约完整演练一单。
- 月/年会员购买、额度发放/扣减/返还/重置、宽限期、续费失败和退款回收完成预发布验证。
- 年度报告生成、免费预览、订单/会员解锁、历史版本、下载、公开分享和撤销完整演练一份。
- 非管理员访问全部 `/admin/*` 和 `/api/admin/*` 返回 404；管理员可从正式导航进入驾驶舱、审计、用户、赛马、互动、视频、纪念和商业后台。
- 管理停用与主动注销互不混用；停用关闭公开分享，恢复不复活注销资产；全部人工操作可在统一审计中按原因追溯。
