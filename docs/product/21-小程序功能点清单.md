# 小程序功能点清单（逐项修改执行清单）

盘点自 2026-08-19 的工作区代码。**组织维度固定为小程序**：按 `app.json` 的 `pages` 顺序分页，每页列出其功能点并编号；凡在管理后台或 Web 端有对应实现的，在「关联端与位置」列标出。

本文件只回答「有哪些功能点、改一项要动哪几端」。功能口径以 [`07-functional-backlog.md`](07-functional-backlog.md) 为准，**Web/后台/REST/玩法 manifest 的清单见** [`15-功能入口清单.md`](15-功能入口清单.md)（那份不再重复小程序侧，两份互补不重叠），UI 约定见 [`../ui-refactor/阶段0-代码盘点与方向分配.md`](../ui-refactor/阶段0-代码盘点与方向分配.md)。

支付相关入口已经有页面和接口，因此仍列为“已实现基线”；但虚拟支付与 `growth_orders` 回调发权益尚未完成发布级改造。涉及 MP-04.3、MP-11.7、MP-13.8、MP-20.3/20.8、MP-21.3 时必须同时看 [`07-functional-backlog.md`](07-functional-backlog.md) 当前待办与 [`23-虚拟支付合规改造方案.md`](23-虚拟支付合规改造方案.md)，不能把“按钮能点”当作“支付可上线”。

## 用法

- **编号唯一且稳定**：`MP-<页序>.<项序>`，页序按 `app.json` 顺序（P00 为跨页框架层）。新增功能点在所属页尾部追加序号，**不重排已有编号** —— 编号是进度依据。
- 本表列出的功能点都是**当前已实现基线**；状态列只跟踪下一轮逐项修改。留空＝未进入本轮修改，`进行中`，`OK`＝本轮已改完并验证，`跳过`＋一句理由。留空不再表示功能未实现。
- **逐项顺序推进，不批量并行**。改完一项跑对应门禁再动下一项：小程序侧 `cd apps/miniprogram && pnpm validate`，平台侧 `cd apps/platform && pnpm check`。
- **「关联端与位置」非空的项不能只改小程序侧**。该列标注的三类端：
  - **Web 应用** = `apps/platform`（Web/H5 同构页面，与小程序共用同一套 REST 接口）
  - **后台** = `apps/platform/src/app/admin/**`（9 个工作台）
  - **官网** = `apps/website`（Astro 静态站，独立域名，只做对外介绍，不调业务接口）
- 该列写「—」表示确认小程序独有，改动不外溢。

## 目录

| 页序  | 页面                               | 功能点数 |
| --- | -------------------------------- | ---- |
| P00 | 跨页框架层（非页面）                       | 6    |
| P01 | `pages/index` 玩法（tab）            | 9    |
| P02 | `pages/create` 制作作品              | 11   |
| P03 | `pages/works` 作品柜（tab）           | 5    |
| P04 | `pages/work` 作品详情                | 10   |
| P05 | `pages/orders` 订单与退款             | 3    |
| P06 | `pages/me` 我的（tab）               | 5    |
| P07 | `pages/pets` 宠物档案                | 8    |
| P08 | `pages/photos` 照片库               | 4    |
| P09 | `pages/account` 账户与隐私            | 3    |
| P10 | `pages/timeline` 成长时间线           | 5    |
| P11 | `pages/health` 健康助手              | 8    |
| P12 | `pages/ai-create` AI 模板创建        | 6    |
| P13 | `pages/ai-run` AI 任务与候选          | 11   |
| P14 | `pages/interactive-create` 创建互动页 | 5    |
| P15 | `pages/interactive` 互动页          | 7    |
| P16 | `pages/video-create` 创建短片        | 6    |
| P17 | `pages/video` 视频项目               | 6    |
| P18 | `pages/memorials` 纪念空间           | 5    |
| P19 | `pages/memorial-share` 纪念页访客视图   | 3    |
| P20 | `pages/commerce` 会员与年度报告         | 8    |
| P21 | `pages/physical` 实体纪念品           | 4    |
| P22 | `pages/login` 登录与退出              | 6    |
| P23 | `pages/theme` 外观主题               | 3    |
| P24 | `island/index` 宠物小岛（分包）          | 11   |
| P25 | `island/avatar` 小岛形象（分包）         | 6    |
| P26 | `island/diary` 小岛日记（分包）          | 5    |

合计 26 页（主包 23 + `island` 分包 3）+ 框架层，**169 个功能点**。

**P24–P26 是分包页面**，路径不在 `app.json` 的 `pages` 里而在 `subPackages[0].pages`，页序接在主包之后（编号稳定不重排，见 `22` 号文 9.4 待定项 11 的建议）。分包页面**已进 `validate.js` 门禁**（第 0 步改的就是这件事），另有岛专属门禁 16/17。

---

## P00 跨页框架层

不属于单页，但任一项改动会波及全部 23 页，所以单列一组、排在最前。

| 编号      | 功能点                                                                                         | 小程序位置                                                  | 关联端与位置                                                | 状态  |
| ------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ----------------------------------------------------- | --- |
| MP-00.1 | 自定义 tabBar 三项（玩法/作品/我的），组件自行订阅主题并注入变量串                                                      | `custom-tab-bar/index.js`、`app.json` 的 `tabBar.custom` | Web 应用 `components/bottom-nav.tsx`                    |     |
| MP-00.2 | 主题系统四层：token 真源、皮肤清单、单例 manager、页面 mixin；`page-meta` 注入变量，导航栏 `onShow` 同步                   | `theme/{tokens,index,manager,page-mixin}.js`           | —                                                     |     |
| MP-00.3 | 十项样式门禁 + `node --test`（零硬编码、token 完整性、对比度、注入串 2KB、组件注册、标签闭合等）                               | `scripts/validate.js`                                  | 后台无；当前无 CI workflow，相关改动与发布前手工执行 `pnpm validate`      |     |
| MP-00.4 | API 层：统一 `{ data }` 解包、`Bearer` 会话头、`x-petbaby-client: miniprogram`、`requestWithRetry` 退避重试 | `services/api.js`                                      | Web 应用 `src/lib/api.ts`（同构，无 client 头）                |     |
| MP-00.5 | 微信静默登录：`wx.login` → `/api/auth/wechat`，账号密码会话优先不被覆盖                                         | `app.js` 的 `onLaunch`                                  | Web 应用无微信登录（走 `/login` 账号密码）；后台 `/admin/users` 可查账号状态 |     |
| MP-00.6 | 陪伴天数与里程碑的端上唯一实现（纪念阶段按 `memorialSince` 封口、不递增）                                               | `services/companion.js`                                | Web 应用与服务端共用 `src/domain/companion.ts`；**两处口径必须一致**   |     |

---

## P01 `pages/index` 玩法（tab 1）

首屏顺序是**先宠物再货架**（改造项 E1）。

| 编号      | 功能点                                                              | 小程序位置                                  | 关联端与位置                                                                                                                              | 状态  |
| ------- | ---------------------------------------------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | --- |
| MP-01.1 | 默认宠物 Hero 卡：封面 + 陪伴天数 + 名字，点击进时间线（带 `petId`）                     | `loadPet()`、`openTimeline()`           | Web 应用**无此区块**（`app/page.tsx` 首屏是标语 Hero + 玩法网格）                                                                                    |     |
| MP-01.2 | 今日里程碑提示，仅当天出现一次，不常驻                                              | `milestone`、`companion.milestoneToday` | Web 应用无                                                                                                                             |     |
| MP-01.3 | 去年今日区块，命中才渲染、未命中静默隐藏                                             | `loadOnThisDay()`                      | Web 应用 `app/page.tsx` 有同构区块（`findOnThisDay`）；改文案要两端同步                                                                               |     |
| MP-01.4 | 无宠物时的标语 Hero（含主题切换入口迁移逻辑）                                        | `index.wxml` 的 `wx:if="{{!pet}}"`      | Web 应用 `app/page.tsx` 的 `.hero`                                                                                                     |     |
| MP-01.5 | 玩法货架：Hero 大卡（16:10）+ 2 列 3:4 网格，价格浮标，无样例图时退回文字卡                  | `splitHero()`、`index.wxml`             | Web 应用 `app/page.tsx` 的 `.plugin-grid`；后台 `/admin/plugins`（manifest 发布回滚）、`/admin/experiments`（赛马变体）；官网 `#plays`、`#services` 玩法介绍文案 |     |
| MP-01.6 | 按 `category` 分流跳转（ai-image/interactive/video/memorial/report/其余） | `start()`                              | Web 应用 `app/page.tsx` 同一套分流表；**新增 category 要两端同改**                                                                                  |     |
| MP-01.7 | 埋点：`visited`（onShow）、`plugin_selected`（点卡）                       | `/api/events` 两处调用                     | 后台 `/admin/operations` 漏斗事件、`/admin/dashboard` 玩法表现                                                                                 |     |
| MP-01.8 | 主题切换入口（有宠物时挂 `hero-top`，无宠物时挂标语 Hero 右上）                         | `openTheme()`                          | —                                                                                                                                   |     |
| MP-01.9 | 加载骨架 / 错误提示 / 空态「玩法正在上架」+ 重新加载                                   | `index.wxml`                           | Web 应用无空态分支                                                                                                                         |     |

---

## P02 `pages/create` 制作作品

四步：填档案 → 选照片 → 生成中 → 完成。前两步走玻璃面板沉浸表单，后两步走常规版式。

| 编号       | 功能点                                                         | 小程序位置                                                        | 关联端与位置                                                                        | 状态  |
| -------- | ----------------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------- | --- |
| MP-02.1  | 初始化：按 `pluginId` 取 manifest + 宠物列表，恢复本地草稿并直接落到对应步骤          | `onLoad()`、`wx.getStorageSync("petbaby_create_*")`           | Web 应用 `components/create-flow.tsx`（无本地草稿恢复）                                  |     |
| MP-02.2  | 四步步骤条与 `formStage` 判定（只有填表两步进玻璃面板）                          | `syncLabels()`、`FORM_STAGES`                                 | Web 应用 `create-flow.tsx` 分步 UI 结构不同                                           |     |
| MP-02.3  | 新建宠物档案（名字/种类/性别/日期类型/日期）并保存                                 | `savePet()` → `POST /api/pets`                               | Web 应用 `create-flow.tsx`、`components/pets-client.tsx`                         |     |
| MP-02.4  | 从已有档案里选一只（picker）                                           | `choosePet()`                                                | Web 应用 `create-flow.tsx`                                                      |     |
| MP-02.5  | 照片九宫格：图库已有 + 本次新拍/新选合并，按 manifest 的 `photos.max` 限量，点选切换/移除 | `syncTiles()`、`toggleTile()`、`choosePhotos()`（压缩 1800px/q82） | Web 应用 `create-flow.tsx` 的文件选择（无 `wx.compressImage` 等价物）                      |     |
| MP-02.6  | 照片上传：逐张 `wx.uploadFile`，失败重试 2 次，合并上传进度                     | `uploadOne()` → `POST /api/uploads`                          | Web 应用 `create-flow.tsx` 上传；服务端 `storage/index.ts` 的 `inspectImage` 魔数校验      |     |
| MP-02.7  | 玩法参数表单：身份证证件类型 / 海报风格+构图+影评 / 画册主题+封面标题                     | `create.wxml` 的三段 `wx:if`                                    | Web 应用 `create-flow.tsx` 同套参数；后台 `/admin/plugins` 改 manifest 会影响可选项           |     |
| MP-02.8  | **制作前**展示解锁价与档位 + 「再攒多少进下一档」（免费玩法整块隐藏）                      | `loadPricing()`、`nextTierText()` → `/api/pets/[id]/pricing`  | Web 应用 `create-flow.tsx` 有同构展示；服务端 `domain/pricing.ts` 是唯一计价源                 |     |
| MP-02.9  | 提交生成（带 `idempotencyKey`）→ 退避轮询 → 结果卡（clay 变体）→ 查看作品 / 再做一个  | `generate()`、`poll()`、`openWork()`                           | Web 应用 `create-flow.tsx`；后台 `/admin/operations` 失败任务、`/admin/dashboard` 待处理队列 |     |
| MP-02.10 | 生成失败提示「免费次数已返还」并退回选照片步骤                                     | `poll()` 的 failed 分支                                         | 服务端 `generation-worker.ts` 退还 `daily_quotas` 并写站内通知（对应 MP-06.3）               |     |
| MP-02.11 | 埋点 `previewed`（生成成功时）                                       | `poll()` 内 `/api/events`                                     | 后台 `/admin/operations` 漏斗事件                                                   |     |

---

## P03 `pages/works` 作品柜（tab 2）

| 编号      | 功能点                                         | 小程序位置                                            | 关联端与位置                                                | 状态  |
| ------- | ------------------------------------------- | ------------------------------------------------ | ----------------------------------------------------- | --- |
| MP-03.1 | 三筛选横排：宠物 / 玩法 / 状态（全部·生成中·生成失败·待解锁·已解锁·已过期） | `choosePet/choosePlugin/chooseStatus`、`filter()` | Web 应用 `components/works-client.tsx`                  |     |
| MP-03.2 | 进行中任务置顶（排队/生成中显进度，失败给「重新尝试」跳回 create）       | `works.wxml` 的 `tasks` 区、`retry()`               | Web 应用 `works-client.tsx`；后台 `/admin/operations` 失败任务 |     |
| MP-03.3 | 已完成作品网格（3:4 锁比例，封面 + 状态角标 + 编号/版本）          | `works.wxml` 的 `.grid`                           | Web 应用 `works-client.tsx`                             |     |
| MP-03.4 | 空态两态：有筛选条件时引导「清空筛选」，否则「去做一个」（clay 只在真无作品时开） | `handleEmptyAction()`                            | Web 应用无双态空态                                           |     |
| MP-03.5 | 打开作品详情                                      | `open()`                                         | Web 应用 `/works/[id]`                                  |     |

---

## P04 `pages/work` 作品详情

沉浸式玻璃面板（`navigationStyle: custom`，`immersive: true` 跳过导航栏同步）。

| 编号       | 功能点                                                     | 小程序位置                                                               | 关联端与位置                                                                                         | 状态  |
| -------- | ------------------------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --- |
| MP-04.1  | 玻璃面板背景：视频作品用 `background-video` + poster，其余用输出图/原照片     | `work.wxml` 的 `t-glass-sheet`                                       | Web 应用 `components/work-detail-client.tsx`（无玻璃面板形态）                                            |     |
| MP-04.2  | 未解锁水印提示（实底容器承载，不把字压在图上）                                 | `slot="overlay"`                                                    | Web 应用 `work-detail-client.tsx` 同义提示                                                           |     |
| MP-04.3  | 事实区：宠物 / 玩法 / 创建时间 / 作品编号                               | `facts` 区、`formatMoment()`                                          | Web 应用 `work-detail-client.tsx`                                                                |     |
| MP-04.4  | 权威声明段（manifest 提供时才出现）                                  | `work.authority`                                                    | Web 应用 `work-detail-client.tsx`；后台 `/admin/plugins` 改 manifest                                 |     |
| MP-04.5  | 分享：开启 7 天 / 重置链接 / 停止分享（停止走二次确认）                        | `share/resetShare/revoke` → `/api/works/[id]/share`、`/revoke-share` | Web 应用 `work-detail-client.tsx`；**访客页 Web 应用 `/share/[token]`**；后台 `/admin/operations` 分享归因    |     |
| MP-04.6  | 历史版本列表与「恢复」（当前版本标记）                                     | `restore()` → `/api/works/[id]/versions`                            | Web 应用 `work-detail-client.tsx`                                                                |     |
| MP-04.7  | 解锁支付：下单 → `prepare` → `wx.requestPayment`（开发模式直接 `pay`） | `unlock()`                                                          | Web 应用 `work-detail-client.tsx`（走 Web 支付分支）；后台 `/admin/operations` 订单操作、`/admin/business` 权益订单 |     |
| MP-04.8  | 解锁按钮上方的价格与档位提示（价来自 `/api/pets/[id]/pricing`，与下单同一计价函数）  | `loadPricing()`                                                     | Web 应用 `work-detail-client.tsx`；服务端 `domain/pricing.ts`                                        |     |
| MP-04.9  | 保存图片到相册 / 下载 MP4 到相册（带 AI 标识与营销水印的成品字节）                 | `saveImage()`、`downloadVideo()` → `/api/works/[id]/download`        | Web 应用 `work-detail-client.tsx` 下载；服务端 `media/ai-label.ts`                                     |     |
| MP-04.10 | `onShareAppMessage` 转发卡片（标题取作品名）                        | `work.js` 末                                                         | Web 应用无（小程序独有能力）                                                                               |     |

---

## P05 `pages/orders` 订单与退款

| 编号      | 功能点                                | 小程序位置                                         | 关联端与位置                                                              | 状态  |
| ------- | ---------------------------------- | --------------------------------------------- | ------------------------------------------------------------------- | --- |
| MP-05.1 | 订单列表：金额（元，两位小数）、玩法、状态标签、已退金额       | `load()` → `/api/orders/list`                 | Web 应用 `components/orders-client.tsx`；后台 `/admin/operations` 订单操作   |     |
| MP-05.2 | 申请退款（不满意退 50%，二次确认；生成失败为全额自动退不需手动） | `confirmRefund()` → `/api/orders/[id]/refund` | Web 应用 `orders-client.tsx`；后台 `/admin/business` 与 `/admin/audit` 留痕 |     |
| MP-05.3 | 空态「还没有订单」→ 去看看玩法                   | `orders.wxml`                                 | Web 应用无空态分支                                                         |     |

---

## P06 `pages/me` 我的（tab 3）

| 编号      | 功能点                                                                               | 小程序位置                                        | 关联端与位置                                                                               | 状态  |
| ------- | --------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------ | --- |
| MP-06.1 | 默认宠物 Hero（封面 + 陪伴天数 + 作品/照片/纪念日统计条），失败静默不挡下方功能                                    | `loadHero()`                                 | Web 应用 `/me` **无此区块**（纯入口列表）                                                         |     |
| MP-06.2 | 今日免费额度环形进度 + 显示名 + 「N 笔退款处理中」标记                                                   | `me.wxml` 的 `.quota` → `/api/account/status` | Web 应用 `components/user-status-client.tsx`                                           |     |
| MP-06.3 | 站内通知列表（生成失败退还、健康提示等由 Worker 写入）                                                   | `status.notifications`                       | Web 应用 `user-status-client.tsx`；服务端 `health/reminders.ts`、`generation-worker.ts` 写入方 |     |
| MP-06.4 | 常用功能入口 7 项：订单 / 宠物档案 / 照片库 / 健康助手 / AI 互动与视频（回首页货架，须 `switchTab`）/ 纪念空间 / 会员与年度报告 | `openOrders` … `openCommerce`                | Web 应用 `/me` 的 `.settings-list`（11 项，含时间线、短片、常见错误指引、运营后台）；**两端入口集合不同，改动要按端各自核对**     |     |
| MP-06.5 | 设置区 3 项：外观主题（右侧显示当前主题名）/ 账户与隐私 / 登录与退出                                            | `openTheme/openAccount/openLogin`            | Web 应用 `/me` 有账户与登录，**无主题项**                                                         |     |

---

## P07 `pages/pets` 宠物档案

| 编号      | 功能点                                              | 小程序位置                                            | 关联端与位置                                            | 状态  |
| ------- | ------------------------------------------------ | ------------------------------------------------ | ------------------------------------------------- | --- |
| MP-07.1 | 档案列表：头像（无图给爪印占位）、默认标记、陪伴天数、种类·生命阶段、生日/到家日        | `reload()`                                       | Web 应用 `components/pets-client.tsx`               |     |
| MP-07.2 | 统计条：作品 / 照片 / 纪念日三数字等分                           | `pets.wxml` 的 `.stats`                           | Web 应用 `pets-client.tsx`                          |     |
| MP-07.3 | 编辑抽屉：名字 + 四个 chip 枚举（种类/性别/日期类型/生命阶段）+ 日期 picker | `edit()`、`chooseSpecies` 等（下标取自 `dataset.index`） | Web 应用 `pets-client.tsx`（表单控件形态不同）                |     |
| MP-07.4 | 更换头像（相册/拍照 → `POST /api/pets/[id]/avatar`）       | `avatar()`                                       | Web 应用 `pets-client.tsx`                          |     |
| MP-07.5 | 设为默认宠物                                           | `setDefault()` → `POST /api/pets/[id]`           | Web 应用 `pets-client.tsx`                          |     |
| MP-07.6 | 删除档案（二次确认，说明照片作品一并隐藏、订单保留）                       | `confirmRemove()` → `DELETE /api/pets/[id]`      | Web 应用 `pets-client.tsx`；后台 `/admin/users` 可见账号数据 |     |
| MP-07.7 | 时间线入口，**必带 `petId`**                             | `timeline()`                                     | Web 应用 `/timeline?petId=`                         |     |
| MP-07.8 | 纪念空间入口，**仅 `senior`/`memorial` 出现**，只改可达性不加推送    | `memorial()`、`showMemorial`                      | Web 应用 `/memorials` 入口在 `/me`，**未按生命阶段收敛**        |     |

---

## P08 `pages/photos` 照片库

| 编号      | 功能点                          | 小程序位置                                                               | 关联端与位置                                       | 状态  |
| ------- | ---------------------------- | ------------------------------------------------------------------- | -------------------------------------------- | --- |
| MP-08.1 | 宠物切换（picker，默认取 `isDefault`） | `choosePet()`                                                       | Web 应用 `components/photo-library-client.tsx` |     |
| MP-08.2 | 照片九宫格 + 点击预览（非管理态）           | `t-photo-grid` 的 `previewable`                                      | Web 应用 `photo-library-client.tsx`            |     |
| MP-08.3 | 管理模式：批量勾选 + 底部固定删除条 + 二次确认   | `toggleManage/togglePick/confirmRemove` → `DELETE /api/photos/[id]` | Web 应用 `photo-library-client.tsx`            |     |
| MP-08.4 | 空态「照片库还是空的」→ 去做一个            | `photos.wxml`                                                       | Web 应用无空态分支                                  |     |

---

## P09 `pages/account` 账户与隐私

| 编号      | 功能点                               | 小程序位置                                  | 关联端与位置                                                  | 状态  |
| ------- | --------------------------------- | -------------------------------------- | ------------------------------------------------------- | --- |
| MP-09.1 | 修改显示名称（展示在作品与分享页上）                | `save()` → `PATCH /api/account`        | Web 应用 `components/account-client.tsx`                  |     |
| MP-09.2 | 导出我的数据（JSON，`wx.openDocument` 打开） | `exportData()` → `/api/account/export` | Web 应用 `account-client.tsx`                             |     |
| MP-09.3 | 危险区：删除账户（二次确认，说明订单退款按法规保留）        | `doDelete()` → `/api/account/delete`   | Web 应用 `account-client.tsx`；后台 `/admin/users` 账号状态与操作历史 |     |

---

## P10 `pages/timeline` 成长时间线

排序键一律 `coalesce(shot_at, created_at)`；「第 N 天」由服务端算好下发，端上不重算。

| 编号      | 功能点                                                        | 小程序位置                                     | 关联端与位置                                                                 | 状态  |
| ------- | ---------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------- | --- |
| MP-10.1 | 进入时优先看 `options.petId` 那一只，其次默认宠物；多只时给切换器                  | `onLoad()`、`choosePet()`                  | Web 应用 `components/timeline-client.tsx`                                |     |
| MP-10.2 | 「已经走过」里程碑列表（只列已达成，100/365/1000，不含第 1 天）                    | `milestones`                              | Web 应用 `timeline-client.tsx`；服务端 `domain/companion.ts`                 |     |
| MP-10.3 | 按年份分组的时间线节点：照片、第 N 天、日期、`dateSource === 'upload'` 标注、里程碑标签 | `load()` 的分组逻辑                            | Web 应用 `timeline-client.tsx`；服务端 `timeline-service.ts`、`media/exif.ts` |     |
| MP-10.4 | 叙事年度短片入口：年份 chip（今年/去年）+ 时长 chip（10/20/30 秒）+ 入队（不在本页轮询）   | `createFilm()` → `POST /api/annual-films` | Web 应用 `timeline-client.tsx` 有同构入口；后台 `/admin/video` 视频任务              |     |
| MP-10.5 | 空态「还没有照片」→ 去上传照片；空态时短片入口整块隐藏                               | `timeline.wxml`                           | Web 应用 `timeline-client.tsx`                                           |     |

---

## P11 `pages/health` 健康助手

**做分诊不做诊断，这是法律要求不是产品选择。** 红线全文见 [`16-竞品分析与产品复盘.md`](16-竞品分析与产品复盘.md) 3.8。改这一页任何一项前先读那一节。

| 编号      | 功能点                                                                              | 小程序位置                                                             | 关联端与位置                                                                         | 状态  |
| ------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------ | --- |
| MP-11.1 | 宠物 chip 选择，**`memorial` 宠物从列表过滤掉**（服务端另有 `HEALTH_UNAVAILABLE_MEMORIAL`，两处都要）     | `load()` 的 `filter`                                               | **Web 应用无健康页**；服务端 `health-service.ts`                                         |     |
| MP-11.2 | 症状描述（≥4 字）→ 提交分诊                                                                 | `submit()` → `POST /api/health-sessions`                          | Web 应用无；服务端 `health/triage.ts`（`EMERGENCY_PATTERNS` 关键词直通、`mentionsDrug` 后置过滤） |     |
| MP-11.3 | 结论区：免责声明（服务端下发，端上不写死）→ 四档紧急度 + AI 生成标记 → 摘要 → 可能相关 → 立即就医条件 → 就医前准备；**与结论同屏不折叠** | `health.wxml` 的 `result` 卡                                        | Web 应用无；服务端 `sanitizeAdvisory`                                                 |     |
| MP-11.4 | 体重记录：公斤输入 → 克存储，称重日期；趋势句与提示语由服务端下发（只陈述变化，不做评价）                                   | `saveWeight()`、`loadWeights()` → `/api/pets/[id]/weights`         | Web 应用无；服务端 `domain/weight-trend.ts`                                           |     |
| MP-11.5 | 最近几次体重列表（≥1000 克显公斤，否则显克）                                                        | `recentWeights`                                                   | Web 应用无                                                                        |     |
| MP-11.6 | 免疫与驱虫记录：四种类型 + **项目名由用户自填（不给候选清单）** + 这次日期 + 下次到期（可留空即不提醒）+ 删除                   | `saveCare()`、`deleteCare()`、`loadCare()`                          | Web 应用无；服务端 `health/reminders.ts` 到期提示走站内通知（→ MP-06.3）                         |     |
| MP-11.7 | 健康档案 PDF：导出 + 已导出列表 + 下载打开（不可分享；无权益时 402 透出单买价，不静默给残缺版）                          | `exportDocument()`、`downloadDocument()` → `/api/health-documents` | Web 应用无；服务端 `health/document.ts`；后台 `/admin/business` 权益订单                     |     |
| MP-11.8 | 历史分诊记录列表（档位标签 + 日期 + 原始描述）；无档案时空态引导建档                                            | `sessions`、`goWeights()`                                          | Web 应用无                                                                        |     |

---

## P12 `pages/ai-create` AI 模板创建

| 编号      | 功能点                                                                                               | 小程序位置                                                             | 关联端与位置                                                                          | 状态  |
| ------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------- | --- |
| MP-12.1 | 宠物选择（默认取 `isDefault`）                                                                             | `choosePet()`                                                     | Web 应用 `components/ai-create-client.tsx`                                        |     |
| MP-12.2 | 宠物身份照单选；切宠物时重新加载，空态引导去照片库                                                                         | `togglePhoto()`、`loadPhotos()`                                    | Web 应用 `ai-create-client.tsx`                                                   |     |
| MP-12.3 | 已登记 9 个玩法入口的横向货架；公开接口当前只返回含 live 模板的 8 个入口，切入口时自动选中第一张 live 模板                                    | `chooseEntry()`、`entries`                                         | Web 应用 `ai-create-client.tsx`；服务端 `image-template-registry.ts`                  |     |
| MP-12.4 | 模板样图横向选择，展示单宠 / 人宠 / 宠物人化主体模式；只下发已有冻结母版的 live 模板                                                  | `chooseTemplate()`、`templates`                                    | Web 应用 `ai-create-client.tsx`；`GET /api/image-templates` 与 `/sample`            |     |
| MP-12.5 | 人宠模板专属：主人授权确认、私有照片上传/预览/单选/删除；单宠与宠物人化模板不显示主人控件                                                    | `toggleAuthorization()`、`uploadOwnerPhoto()`、`removeOwnerPhoto()` | Web 应用 `ai-create-client.tsx`；服务端 `owner-photo-service.ts`、`/api/owner-photos*` |     |
| MP-12.6 | 提交候选任务：固定 `templateId`、1 张宠物身份照；人宠模板另带 1 张已授权主人照片；普通模板生成 4 张，宠物人化按“宠物图一 + 效果图二”生成 2 张，均跳 `ai-run` | `create()` → `POST /api/ai-runs`                                  | Web 应用 `/ai/create`；后台 `/admin/operations` AI 成本/Provider/熔断、AI 任务              |     |

---

## P13 `pages/ai-run` AI 任务与候选

| 编号       | 功能点                                                                      | 小程序位置                                                          | 关联端与位置                                                      | 状态  |
| -------- | ------------------------------------------------------------------------ | -------------------------------------------------------------- | ----------------------------------------------------------- | --- |
| MP-13.1  | 状态轮询（1.6 秒）+ 下拉刷新 + 离页清理定时器                                              | `poll()`、`onPullDownRefresh()`、`onUnload()`                    | Web 应用 `components/ai-results-client.tsx`                   |     |
| MP-13.2  | 排队/生成中：排队位次、预计秒数、尝试次数、可先退出                                               | `ai-run.wxml` 的 queued/processing 卡                            | Web 应用 `ai-results-client.tsx`                              |     |
| MP-13.3  | 取消任务（二次确认，额度返还）                                                          | `cancel()` → `PATCH action: cancel`                            | Web 应用 `ai-results-client.tsx`                              |     |
| MP-13.4  | 失败态：错误码 + 重试一次（额度已返还）                                                    | `retry()` → `PATCH action: retry`                              | Web 应用 `ai-results-client.tsx`；后台 `/admin/operations` AI 任务 |     |
| MP-13.5  | 已取消态说明（无订单、无扣额）                                                          | `ai-run.wxml` 的 cancelled 卡                                    | Web 应用 `ai-results-client.tsx`                              |     |
| MP-13.6  | 候选网格：普通模板四选一、宠物人化二选一，均支持选中标记（**刻意不走沉浸式**，全屏背景与并排比较冲突）                    | `select()` → `PATCH action: select`                            | Web 应用 `ai-results-client.tsx`                              |     |
| MP-13.7  | 普通模板定向重抽一组：主人不像 / 宠物不像 / 构图偏离；“主人不像”只对人宠显示，已生成作品后禁用。宠物人化不显示重抽入口，服务端也拒绝重抽 | `chooseRerollReason()`、`reroll()` → `/api/ai-runs/[id]/reroll` | Web 应用 `ai-results-client.tsx`                              |     |
| MP-13.8  | 解锁选中的那张：`unlock` → 订单 `prepare` → 支付                                     | `unlock()`                                                     | Web 应用 `ai-results-client.tsx`；后台 `/admin/operations` 订单操作  |     |
| MP-13.9  | 已选中后的沉浸式确认区（选中图占满全屏 + 事实区 Provider/模型；普通模板显示剩余重抽，宠物人化显示 2 张候选）           | `deriveRun()` 的 `immersive`                                    | Web 应用无沉浸形态                                                 |     |
| MP-13.10 | 保存高清图到相册（仅已解锁；预览与正式都从已打标字节缩）                                             | `save()`                                                       | Web 应用下载；服务端 `media/ai-label.ts`                            |     |
| MP-13.11 | 创建 7 天分享链接（复制到剪贴板）+ 查看作品档案                                               | `share()`、`openWork()`                                         | Web 应用 `/share/[token]` 访客页；后台 `/admin/operations` 分享归因     |     |

---

## P14 `pages/interactive-create` 创建互动页

| 编号      | 功能点                                    | 小程序位置                                         | 关联端与位置                                                                   | 状态  |
| ------- | -------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------ | --- |
| MP-14.1 | 宠物选择                                   | `choosePet()`                                 | Web 应用 `/interactive/create`                                             |     |
| MP-14.2 | 场景照片选择 ≤6 张（有序），空态引导                   | `togglePhoto()`                               | Web 应用 `/interactive/create`                                             |     |
| MP-14.3 | 标题（≤60）与页面文案（≤180）                     | `inputTitle()`、`inputCopy()`                  | Web 应用 `/interactive/create`                                             |     |
| MP-14.4 | 场景氛围预设选择 + 实时预览（**场景配色是内容属性，与全局主题隔离**） | `chooseScene()`、`theme/scene-presets.js`      | Web 应用 `components/interactive-canvas.tsx`；后台 `/admin/memorials` 主题与素材版本 |     |
| MP-14.5 | 创建互动页并跳管理页                             | `create()` → `POST /api/interactive-sessions` | Web 应用 `/interactive/create`；后台 `/admin/interactive` 会话列表                |     |

---

## P15 `pages/interactive` 互动页

一页两态：`token` 参数存在即访客模式，否则作者模式。

| 编号      | 功能点                                                         | 小程序位置                                                     | 关联端与位置                                                                                           | 状态  |
| ------- | ----------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | --- |
| MP-15.1 | 场景本体渲染 + 轻点收集星尘（访客即时上报，作者态落库）                               | `addStar()`                                               | Web 应用 `components/interactive-studio-client.tsx`、`interactive-guest-client.tsx`                 |     |
| MP-15.2 | 编辑区展开/收起：照片改选、标题文案、场景切换、保存                                  | `toggleEditing()`、`save()` → `PATCH`                      | Web 应用 `interactive-studio-client.tsx`                                                           |     |
| MP-15.3 | 开启 7 天公开分享（复制链接）/ 停止分享（二次确认）                                | `share()`、`revoke()`                                      | Web 应用 `/interactive/share/[token]` 访客页；后台 `/admin/interactive`                                  |     |
| MP-15.4 | 导出 15 秒 MP4 + 导出进度轮询                                        | `exportVideo()` → `/api/interactive-sessions/[id]/export` | Web 应用 `interactive-studio-client.tsx`；后台 `/admin/interactive` 导出任务、`/admin/video`               |     |
| MP-15.5 | 查看归档作品                                                      | `openWork()`                                              | Web 应用 `/works/[id]`                                                                             |     |
| MP-15.6 | 访客模式：只读渲染 + `visit` 埋点 + 离页 `duration` 埋点（`visitorKey` 持久化） | `load()`、`onUnload()` → `/api/interactive-share/[token]`  | Web 应用 `interactive-guest-client.tsx`、`components/share-tracker.tsx`；后台 `/admin/operations` 分享归因 |     |
| MP-15.7 | `onShareAppMessage`（有 token 走访客路径，否则作者路径）                   | `interactive.js` 末                                        | Web 应用无                                                                                          |     |

---

## P16 `pages/video-create` 创建短片

| 编号      | 功能点                                 | 小程序位置                                   | 关联端与位置                                                                                     | 状态  |
| ------- | ----------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------ | --- |
| MP-16.1 | 宠物选择                                | `choosePet()`                           | Web 应用 `components/video-create-client.tsx`                                                |     |
| MP-16.2 | 项目名称 + 片尾字幕                         | `inputTitle()`、`inputCaption()`         | Web 应用 `video-create-client.tsx`                                                           |     |
| MP-16.3 | 成片时长三档 10/20/30 秒，chip 上直接标「最多 N 张」 | `chooseDuration()`、`DURATION_OPTIONS`   | **服务端单一事实源 `domain/video-duration.ts`，本页是对照实现，改档位必须两边同步**；Web 应用 `video-create-client.tsx` |     |
| MP-16.4 | 背景音乐三选（不加/安静/明亮）                    | `chooseBgm()`                           | Web 应用 `video-create-client.tsx`；后台 `/admin/video` 目录版本                                    |     |
| MP-16.5 | 照片按选择顺序排片，超上限明确报错而非静默截断；实时显示每张停留秒数  | `toggle()`、`syncDuration()`             | Web 应用 `video-create-client.tsx`                                                           |     |
| MP-16.6 | 创建项目并跳编辑页                           | `create()` → `POST /api/video-projects` | Web 应用 `/video/create`；后台 `/admin/video` 视频任务                                              |     |

---

## P17 `pages/video` 视频项目

| 编号      | 功能点                     | 小程序位置                                          | 关联端与位置                                                   | 状态  |
| ------- | ----------------------- | ---------------------------------------------- | -------------------------------------------------------- | --- |
| MP-17.1 | 项目详情 + 当前渲染状态轮询（2.5 秒）  | `load()`                                       | Web 应用 `components/video-project-client.tsx`             |     |
| MP-17.2 | 渲染进度卡（仅有任务时出现，避免空进度条误导） | `video.wxml` 的 queued/processing 卡             | Web 应用 `video-project-client.tsx`                        |     |
| MP-17.3 | 片尾字幕编辑 + 保存草稿           | `inputCaption()`、`save()` → `PATCH`            | Web 应用 `video-project-client.tsx`                        |     |
| MP-17.4 | 提交渲染                    | `render()` → `/api/video-projects/[id]/render` | Web 应用 `video-project-client.tsx`；后台 `/admin/video` 视频任务 |     |
| MP-17.5 | 取消渲染（二次确认）/ 失败重试        | `cancel()`、`retry()`                           | Web 应用 `video-project-client.tsx`；后台 `/admin/video`      |     |
| MP-17.6 | 查看作品与解锁                 | `openWork()`                                   | Web 应用 `/works/[id]`                                     |     |

---

## P18 `pages/memorials` 纪念空间

`mood: "memorial"`：动效降为 fade、关闭装饰。陪伴天数**过去式且不递增**。

| 编号      | 功能点                                                                     | 小程序位置                                              | 关联端与位置                                                                                                           | 状态  |
| ------- | ----------------------------------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --- |
| MP-18.1 | 时间轴列表：日期节点 + 陪伴天数（截止日取 `memorialSince`，回落空间创建时间）+ 标题 + 场景名 + 生命周期/可见性标签 | `load()`、`formatDate()`                            | Web 应用 `components/memorial-client.tsx`；官网 `#memorial` 星尘纪念介绍                                                    |     |
| MP-18.2 | 新建纪念空间抽屉：宠物 / 标题 / 故事（≤600）/ 照片 ≤9                                      | `create()` → `POST /api/memorials`                 | Web 应用 `memorial-client.tsx`；后台 `/admin/memorials` 空间列表                                                          |     |
| MP-18.3 | 生成纪念物操作面板三项：纪念册 / 纪念视频 / 星尘页                                            | `chooseProduct()` → `/api/memorials/[id]/products` | Web 应用 `memorial-client.tsx`；后台 `/admin/memorials` 模板与生成任务；服务端 `toneVariants` 调性解析（archived PL-20/21/22 **不能删**） |     |
| MP-18.4 | 更多菜单：隐藏 / 恢复空间（不删除内容）                                                   | `chooseMore()` → `/api/memorials/[id]/lifecycle`   | Web 应用 `memorial-client.tsx`；后台 `/admin/memorials`、`/admin/audit`                                                |     |
| MP-18.5 | 空态「还没有纪念空间」→ 新建                                                         | `memorials.wxml`                                   | Web 应用 `memorial-client.tsx`                                                                                     |     |

---

## P19 `pages/memorial-share` 纪念页访客视图

| 编号      | 功能点                                                                         | 小程序位置                       | 关联端与位置                                                                  | 状态  |
| ------- | --------------------------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------------- | --- |
| MP-19.1 | 访客渲染：封面式标题 + 宠物名 + 原始比例纵向照片流 + 故事；**固定 `cute` 主题不读访客本地偏好**，`mood: memorial` | `memorial-share.js`、`.wxml` | Web 应用 `/memorial/share/[token]`、`components/memorial-share-client.tsx` |     |
| MP-19.2 | `visit` 埋点上报                                                                | `onLoad()` 内 POST           | Web 应用 `share-tracker.tsx`；后台 `/admin/operations` 分享归因                  |     |
| MP-19.3 | 打不开时空态「这个纪念空间可能已关闭分享」；页尾明示不开放留言/排名/营销弹窗                                     | `.wxml` 的 `t-empty`         | Web 应用 `memorial-share-client.tsx`                                      |     |

---

## P20 `pages/commerce` 会员与年度报告

**套餐名、价格、权益一律从 `/api/membership-plans` 读，端上不留任何套餐常量。**

| 编号      | 功能点                                                        | 小程序位置                                                           | 关联端与位置                                                                                        | 状态  |
| ------- | ---------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --- |
| MP-20.1 | 当前会员状态卡：套餐名（服务端下发 label）、状态、有效期、权益逐条、年报免费解锁余量              | `load()` 的 `memberships` 映射                                     | Web 应用 `components/commerce-client.tsx`；后台 `/admin/business` 会员与额度账本、套餐版本                     |     |
| MP-20.2 | 方案对比卡：价格、「省多少 / 做 N 件回本」提示、「更划算」标签（仅多套餐时给）                 | `plans` 映射                                                      | Web 应用 `commerce-client.tsx`；后台 `/admin/business` 套餐版本                                        |     |
| MP-20.3 | 开通会员并支付                                                    | `member()` → `POST /api/memberships` + `growth-orders/[id]/pay` | Web 应用 `commerce-client.tsx`；后台 `/admin/business` 权益订单                                        |     |
| MP-20.4 | 纪念日提醒订阅（生日）；走 `wx.requestSubscribeMessage` 授权弹层再落库，拒绝要记成拒绝 | `remind()`、`subscribe()`                                        | Web 应用 `commerce-client.tsx`（**无微信授权弹层**）；后台 `/admin/business` 订阅消息；模板 ID 走 `config.local.js` |     |
| MP-20.5 | 「去年今日」推送授权按钮**常驻**（微信订阅消息按次授权，推一次要再授权）                     | `remindOnThisDay()`                                             | Web 应用 `commerce-client.tsx`；后台 `/admin/business` 订阅消息                                        |     |
| MP-20.6 | 退订提醒（二次确认）                                                 | `confirmCancel()` → `DELETE /api/subscriptions/[id]`            | Web 应用 `commerce-client.tsx`                                                                  |     |
| MP-20.7 | 生成年度报告                                                     | `report()` → `POST /api/annual-reports`                         | Web 应用 `commerce-client.tsx`；后台 `/admin/business` 年度报告、报告模板版本                                 |     |
| MP-20.8 | 解锁年度报告高清版：**可能不产生订单**（会员权益命中时服务端直接解锁），按钮不写「支付」             | `unlock()` → `PATCH action: unlock`                             | Web 应用 `commerce-client.tsx`；Web 应用 `/annual-report/share/[token]` 分享页；后台 `/admin/business`   |     |

---

## P21 `pages/physical` 实体纪念品

| 编号      | 功能点                                              | 小程序位置                                                       | 关联端与位置                                                                                                 | 状态  |
| ------- | ------------------------------------------------ | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | --- |
| MP-21.1 | 已解锁作品横向缩略图选择（`/api/works?locked=false`），空态引导去作品柜 | `chooseWork()`                                              | Web 应用 `components/physical-commerce-client.tsx`                                                       |     |
| MP-21.2 | 收货信息五字段（收货人/手机号/省/市/详细地址），全填才可提交                 | `onField()`、`refreshSubmittable()`                          | Web 应用 `physical-commerce-client.tsx`                                                                  |     |
| MP-21.3 | 下单确认弹层（回显作品与地址）→ 创建订单 → 支付                       | `askConfirm()`、`create()` → `/api/physical-orders` + `/pay` | Web 应用 `/commerce/physical`；后台 `/admin/business` 实体订单、`/api/admin/physical-orders/[id]/{status,print}` |     |
| MP-21.4 | 实体订单列表：SKU 名、履约状态八态、物流单号/「等待履约」                  | `load()` 的 `orders` 映射                                      | Web 应用 `physical-commerce-client.tsx`；后台 `/admin/business` 改状态与打印面单                                    |     |

---

## P22 `pages/login` 登录与退出

| 编号      | 功能点                                                    | 小程序位置                                           | 关联端与位置                                     | 状态  |
| ------- | ------------------------------------------------------ | ----------------------------------------------- | ------------------------------------------ | --- |
| MP-22.1 | 品牌区（F 杂志封面式；`cute` 用插画、`light/dark` 用文字、`glass` 用发光标识） | `login.wxml` 的 `.brand`                         | Web 应用 `components/login-client.tsx`（形态不同） |     |
| MP-22.2 | 会话检查：已登录展示当前账号 + 返回首页 + 退出登录                           | `onShow()` → `/api/auth/session`、`/api/account` | Web 应用 `login-client.tsx`                  |     |
| MP-22.3 | 登录/注册分段控件切换                                            | `switchMode()`                                  | Web 应用 `login-client.tsx`                  |     |
| MP-22.4 | 账号密码登录（账号 ≥3 位）                                        | `submit()` → `/api/auth/password/login`         | Web 应用 `login-client.tsx`                  |     |
| MP-22.5 | 注册（密码 ≥10 位含字母数字、昵称选填、邀请码按 `inviteRequired` 出现）        | `submit()` → `/api/auth/password/register`      | Web 应用 `login-client.tsx`；邀请制开关在服务端配置      |     |
| MP-22.6 | 未开启密码登录时的降级卡：引导用微信身份进入                                 | `login.wxml` 的 `!enabled` 分支                    | Web 应用无（Web 无微信登录）                         |     |

---

## P23 `pages/theme` 外观主题

| 编号      | 功能点                                     | 小程序位置                               | 关联端与位置 | 状态  |
| ------- | --------------------------------------- | ----------------------------------- | ------ | --- |
| MP-23.1 | 四套主题预览卡，**每张卡用该主题自己的 token 渲染**（不是当前主题） | `build()`、`manager.getCssVarsFor()` | —      |     |
| MP-23.2 | 切换即时生效、记住选择、不发网络请求                      | `choose()` → `manager.setTheme()`   | —      |     |
| MP-23.3 | 毛玻璃降级说明（部分安卓退回不透明卡片，功能不受影响）             | `theme.wxml` 末 `t-notice`           | —      |     |

---

## P24 `island/index` 宠物小岛（分包）

方案见 [`22-宠物小岛游戏化方案.md`](22-宠物小岛游戏化方案.md)，实现记录与偏离在该文 11.7（端上）与 11.8–11.10（服务端与两侧对接）。**这是仓库第一个留存型模块**，也是第一个 Canvas 页面：场景在 Canvas 里画，HUD 是覆盖其上的 WXML（该文 5.1）。

**服务端 `/api/island/*` 已于 2026-08-05 实现**（9 条路由），两侧已对接。对接时修了四处字段名对不上的地方，共同特征是**不报错**（`eventDate` 实际是 `date`、`run.id` 实际是 `runId`、`/api/media/` 吃对象键而非照片 id、`downloadFile` 漏带鉴权头导致私有立绘 401），逐条见该文 11.10 —— 改岛的端上代码前先看那一章。

| 编号       | 功能点                                                                                                                                 | 小程序位置                                                                                             | 关联端与位置                                   | 状态  |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------- | --- |
| MP-24.1  | Canvas 2D 场景层：底图 → 光照 → 天气 → 物件 → 立绘 → 粒子，帧率上限 30fps、静止即停帧、页面 `onHide` 关待机动画                                                        | `island/scene/renderer.js`                                                                        | —（Web 端不做，`22` 号文 9.4 待定项 10）            |     |
| MP-24.2  | 触摸命中表：Canvas 内无节点，热区自维护，每个 ≥88 设计单位；点空地无反应                                                                                          | `island/scene/layout.js` 的 `buildHitZones`                                                        | —                                        |     |
| MP-24.3  | 两档镜头：全景 / 近景（**同一张立绘放大裁切，不是另一个角度**），点宠物切换                                                                                           | `toggleCamera()`、`layout.petRect()`                                                               | —                                        |     |
| MP-24.4  | 昼夜四档 × 天气四档叠加，跨段 2.4 秒交叉淡入；取值与服务端 `domain/island-weather.ts` 由测试钉死。**粒子/地面反光/窗户暖光跟随同一个 `fade`**，不是只有叠加层在淡（11.13）；暖光与反光按两档权重相加，暮转夜不闪 | `island/scene/ambient.js`、`renderer.js` 的 `paintParticles` / `paintWetGround` / `paintWindowGlow` | 服务端 `src/domain/island-weather.ts`（同一口径） |     |
| MP-24.5  | 待机动画：呼吸 / 浮动 / 挤压拉伸 + 情绪粒子。**不做眨眼和转头**（单张立绘做不到）                                                                                     | `renderer.js` 的 `paintPet` / `emote`                                                              | —                                        |     |
| MP-24.6  | 素材远程加载：`downloadFile` → `saveFile` → LRU 淘汰（**真删文件**，8MB 预算）                                                                        | `island/scene/assets.js`                                                                          | 服务端下发绝对 URL（须补域名）                        |     |
| MP-24.7  | 素材未就绪路径：纯色底 + 立绘，**不画占位色块**；逐张独立成败                                                                                                  | `renderer.js` 的 `paintBase`、`assets.preload()`                                                    | —                                        |     |
| MP-24.8  | HUD 顶部一行：名字 + 陪伴天数 + 昼夜天气，**必须有奶白底板**（门禁 16 钉住其存在）                                                                                  | `index.wxml` 的 `.hud-top`、`island/hud-vars.js`                                                    | —                                        |     |
| MP-24.9  | 三个互动（摸摸 / 找找 / 喂它）：乐观动画立刻播，**掉落物等服务端返回才显示**                                                                                         | `pet()`、`gather()`、`feed()` → `act()`                                                             | 服务端 `POST /api/island/actions`           |     |
| MP-24.10 | 到达每日上限的措辞按动作类型给：「今天的草丛都看过了」而**不是**「体力耗尽」（4.1 #4 的判据在措辞上）                                                                            | `limitHintOf()`                                                                                   | —                                        |     |
| MP-24.11 | 低端机降级：`benchmarkLevel < 10` 或未知时粒子数掉到 40 以内，**且跨档过渡时不画淡出的旧档粒子**（两档同时在场会让峰值 40+40，正好抹掉降级省下的那一半）                                      | `detectDegraded()`、`renderer.setDegraded()`                                                       | —                                        |     |

## P25 `island/avatar` 小岛形象（分包）

| 编号      | 功能点                                         | 小程序位置                      | 关联端与位置                                                            | 状态  |
| ------- | ------------------------------------------- | -------------------------- | ----------------------------------------------------------------- | --- |
| MP-25.1 | 可入岛宠物列表，**`memorial` 端上过滤**（服务端也拦，两处都要）     | `service.selectablePets()` | 服务端 `POST /api/island/pets`                                       |     |
| MP-25.2 | 选参考照。**必须图生图** —— 纯文生图只能得到「某只橘猫」（判据同 PL-10） | `choosePhoto()`、`submit()` | 服务端 `POST /api/island/avatar`                                     |     |
| MP-25.3 | 轮询候选（1.6 秒一次），沿用 `pages/ai-run` 交互          | `poll()`                   | 服务端 `GET /api/island/avatar/[runId]`                              |     |
| MP-25.4 | 四选一 → 选定入岛。**用户确认后才入岛**，允许重生成               | `select()`、`regenerate()`  | 服务端 `.../select`                                                  |     |
| MP-25.5 | 走**独立日额度**，不占做图额度（`22` 号文 6.3）              | 由服务端判定                     | 服务端 `island/avatar.ts` 的 `MAX_AVATAR_RUNS`（每宠 1+2 次，取消的不计入）       |     |
| MP-25.6 | 立绘打 AI 标识但**岛内实时渲染不叠**，只在导出/分享时叠            | 服务端职责，端上只展示已打标字节           | 服务端 `media/ai-label.ts` + `island/avatar.ts`（岛用 `AI_LABEL_PLATE`） |     |

## P26 `island/diary` 小岛日记（分包）

| 编号      | 功能点                                                                                                                                          | 小程序位置                                                  | 关联端与位置                                                                               | 状态  |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------ | --- |
| MP-26.1 | 日记列表，竖轴版式（与 `pages/timeline` 同一处理）                                                                                                           | `diary.wxml` 的 `.line`                                 | 服务端 `GET /api/island/diary`                                                          |     |
| MP-26.2 | 触底翻页 + 下拉刷新；翻到底给「到这儿就是全部了」而不是错误态                                                                                                             | `onReachBottom()`、`loadMore()`、`onPullDownRefresh()`   | 服务端分页 cursor                                                                         |     |
| MP-26.3 | **文案由服务端按模板拼好，端上不加工** —— 端上再加工等于绕过门禁 11–15 的全量扫描                                                                                             | `decorate()` 只加标签与日期                                   | 服务端 `island/diary.ts`                                                                |     |
| MP-26.4 | 四类条目标签：今天 / 里程碑 / 这几天 / 去年今日                                                                                                                 | `KIND_LABEL`                                           | 服务端 `island_events.kind`                                                             |     |
| MP-26.5 | 「已经走过」里程碑区块：100/365/1000，**只列已达成的**（未达成的读作「还差 20 天」，属 4.1 #7 的催促）；文案走 `companion.milestoneLabel`，与服务端逐字一致；**不受空状态影响**（没有日记但已陪伴 100 天的用户要看得到） | `service.reachedMilestones()`、`diary.wxml` 的 `.stones` | 服务端 `GET /api/island` 的 `snapshot.milestones`（`MILESTONE_DAYS`，按 `memorialSince` 封口） |     |

---

## 附录 A：官网（`apps/website`）与小程序功能点的对应

官网是纯静态介绍站，**不调业务接口**，所以它与功能点的关联只有「对外描述是否与实现一致」这一层。改到下列功能点时要顺带核对官网文案：

| 官网区块                              | 对应功能点                   | 核对要点                       |
| --------------------------------- | ----------------------- | -------------------------- |
| `#plays`（Plays.astro）             | MP-01.5                 | 三张卡的玩法名、照片张数、耗时描述          |
| `#services`（Services.astro）       | MP-01.5、MP-12.*、MP-16.* | 图文/AI 肖像/视频短片三类的能力边界与时长档   |
| `#portrait`（PortraitDetail.astro） | MP-12.*、MP-13.6         | 「四张候选选一张」的口径               |
| `#memorial`（MemorialDetail.astro） | MP-18.*                 | 星尘纪念的克制定位                  |
| `#works`（Works.astro）             | MP-12.4                 | 「同一只猫七种风格」——比较型槽位必须同一只样板宠物 |
| Steps.astro                       | MP-02.3、MP-02.5、MP-01.5 | 三步流程与实际步骤一致                |
| Voices.astro                      | MP-04.2、MP-09.3         | 「三个不肯将就的决定」涉及水印与隐私承诺       |
| FOOTER_COLUMNS（`config/site.ts`）  | MP-01.5、MP-18.3         | 玩法与纪念产品的清单项                |
| 全站文案                              | MP-11.*                 | **不得出现「诊断」「确诊」「治愈」「问诊」**   |

官网上线仍缺小程序码（`public/assets/miniprogram-qr.png` + `site.ts` 的 `MINIPROGRAM_QR.available`），三个触点共用。

## 附录 B：服务端已建但小程序无调用方

改动相关功能点时可一并接上，也可维持现状——**先记录，不默认视为缺陷**。

| 接口                                       | 小程序现状                    | 相关功能点            |
| ---------------------------------------- | ------------------------ | ---------------- |
| `/api/notifications/[id]`                | 通知只展示不标记已读               | MP-06.3          |
| `/api/works/[id]` DELETE                 | 作品无删除入口                  | MP-03.3、MP-04.*  |
| `/api/works/[id]/copy`                   | 无「复制一份」入口                | MP-04.*          |
| `/api/share/[token]/qr`、`/scheme`、`/cta` | 分享只复制链接，未用小程序码与跳转 scheme | MP-04.5、MP-13.11 |
| `/api/addresses`、`/api/addresses/[id]`   | 实体下单每次手填，未用地址簿           | MP-21.2          |
| `/api/physical-skus`                     | SKU 与 ¥39.90 写死在页面       | MP-21.3          |
| `/api/video-catalog`                     | BGM 三项写死在页面              | MP-16.4          |
| `/api/memorials/[id]/export`             | 纪念空间无导出入口                | MP-18.*          |
| `/api/annual-reports/[id]/download`      | 年报无下载入口（只有解锁）            | MP-20.8          |
| `/api/annual-report-share/[token]`       | 年报分享页只有 Web 端            | MP-20.8          |
| `/api/interactive-sessions/[id]/events`  | 互动页作者看不到访客数据             | MP-15.6          |

**反向缺口（小程序已建、服务端未实现）**：**当前为空**（2026-08-05）。

原先这里记的是 `island/service.js` 的 9 个方法全部无对应服务端 —— `/api/island/*` 已于 2026-08-05 实现（9 条路由），两侧已对接。**对接时修了四处字段名对不上的地方**，逐条见 `22` 号文 11.10；那四处的共同特征是**不报错**（日期空白、URL 里出现 `undefined`、图片列全裂、私有立绘 401），所以这个方向的缺口值得单列一节 —— 端上先于服务端写时字段名是猜的，而猜错不会有任何报错提示。

## 附录 C：两端功能覆盖差异

| 差异                | 说明                                            | 相关功能点           |
| ----------------- | --------------------------------------------- | --------------- |
| Web 应用无健康助手       | 健康线八个功能点全部只在小程序                               | MP-11.*         |
| Web 应用无主题切换       | 主题系统是小程序独有                                    | MP-23.*、MP-00.2 |
| Web 应用无「先宠物再货架」首屏 | 首页与「我的」都没有默认宠物 Hero                           | MP-01.1、MP-06.1 |
| 小程序无「常见错误指引」      | Web 应用有 `/support`，小程序未接                      | MP-06.4         |
| 小程序无协议三页          | Web 应用有 `/legal/{terms,privacy,refund}`，官网有两页 | MP-22.*         |
| 纪念入口收敛口径不同        | 小程序按生命阶段收敛，Web 应用常驻在 `/me`                    | MP-07.8         |
| Web 应用「我的」入口比小程序多 | 多出时间线、短片、常见错误指引、运营后台                          | MP-06.4         |
